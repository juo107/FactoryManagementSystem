services:
  redis:
    image: redis:latest
    container_name: FactoryManagementSystem-Redis
    ports:
      - "6379:6379"
    restart: always
    command: redis-server --save 60 1 --loglevel warning
    volumes:
      - redis_data:/data

volumes:
  redis_data:
